---
import { ABOUT_ME_LINK } from "@/constants"
import Logo from "./logo/Logo.svelte"
import ThemePicker from "./theme/ThemePicker"
---

<header class="sticky top-0 z-40 border-b bg-base-100 py-2">
  <div class="container flex items-center justify-between">
    <Logo client:load />
    <ul class="flex items-center gap-2">
      <li>
        <a
          class="link-hover link font-semibold normal-case after:content-['_↗']"
          href={ABOUT_ME_LINK}>About me</a
        >
      </li>
      <li>
        <ThemePicker client:load />
      </li>
    </ul>
  </div>
</header>
